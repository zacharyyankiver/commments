export type BuildCommandOptions = {
    name: string;
    command: string;
};
export declare function getAddBuildCommandFn({ name, command }: BuildCommandOptions): (packageJson?: Record<string, any>, additionalProperties?: Record<string, any>) => Record<string, any>;
