import { Channel } from './channel';
import { ConnectMessage, EntryAPI, EntryFieldInfo } from './types';
import { EntryFieldAPI } from './types/field.types';
export default function createEntry(channel: Channel, entryData: ConnectMessage['entry'], fieldInfo: EntryFieldInfo[], createEntryField: (info: EntryFieldInfo) => EntryFieldAPI): EntryAPI;
